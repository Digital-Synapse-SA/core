{
  "config": {
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]",
      "cannot_connect": "Could not connect to controller with URL {base_url}"
    },
    "step": {
      "user": {
        "data": {
          "vera_controller_url": "Controller URL",
          "lights": "Vera switch device IDs to treat as lights in SmartHub.",
          "exclude": "Vera device IDs to exclude from SmartHub."
        },
        "data_description": {
          "vera_controller_url": "It should look like this: http://192.168.1.161:3480"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Vera controller options",
        "description": "See the Vera documentation for details on optional parameters: https://www.smart-hub.io/integrations/vera/. Note: Any changes here require a restart of the SmartHub server. To clear values, provide a space.",
        "data": {
          "lights": "[%key:component::vera::config::step::user::data::lights%]",
          "exclude": "[%key:component::vera::config::step::user::data::exclude%]"
        }
      }
    }
  }
}
